/*jslint indent: 4, browser: true */
/*global $, alert, ApplicationMediator, console, prequire, settings, tileui */

define(function (require) {
    "use strict";
    var App                 = require('app');

    function main() {
        App.start();
    }
    main();
});
